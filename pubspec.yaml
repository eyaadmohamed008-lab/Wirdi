name: quran_app_clean
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  quran: ^1.2.9
  shared_preferences: ^2.2.2
  audioplayers: ^5.2.1
  cupertino_icons: ^1.0.8
  google_fonts: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_launcher_icons: "^0.13.1"

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "icon.png"
  min_sdk_android: 21
  # هنا بنحط اسم التطبيق اللي هيظهر تحت الأيقونة للموبايل
  name: "وِرْدي"

flutter:
  uses-material-design: true
